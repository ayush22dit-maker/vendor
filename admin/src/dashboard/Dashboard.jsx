import React from "react";
import {
    Box,
    Typography
} from "@mui/material";

const Dashboard = () => {
    return (
        <Box>
            <Typography variant="h4">
                Dashboard
            </Typography>

            <Typography>
                Welcome to Vendor Admin Panel
            </Typography>
        </Box>
    );
};

export default Dashboard;